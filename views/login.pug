extends layout

block content
  h1 Connexion

  p Pour vous connecter à l'intranet, entrez votre identifiant et mot de passe.

  form(action="/auth/login" method="POST")
    label(for="email") Email :
    input(type="email" name="email" placeholder="ex: owen.lopez@example.com" required)

    label(for="password") Mot de passe :
    input(type="password" name="password" required)

    button(type="submit") CONNEXION
